# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Resend (transactional email)
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL=NWI Fun Ball <tickets@yourdomain.com>

# Database (Neon Postgres)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Site URL (for Stripe redirects and ticket verification links)
# Prefer SITE_URL (server-only, always read at runtime). NEXT_PUBLIC_SITE_URL is fallback.
SITE_URL=https://yoursite.com
NEXT_PUBLIC_SITE_URL=https://yoursite.com

# Optional: override homepage promo video (default is the NWI Fun Ball Facebook Reel in lib/promo-video.ts)
# Facebook plugin URL, reel/watch URL, YouTube, or direct .mp4/.webm
NEXT_PUBLIC_PROMO_VIDEO_URL=

# Optional: Good Day Northwest Indiana — live stream or show page (opens in new tab)
NEXT_PUBLIC_GOOD_DAY_NWI_URL=

# Stay-connected popup: recipient for mailto flow (default: visionquest2@outlook.com)
NEXT_PUBLIC_STAY_CONNECTED_EMAIL=

# Stripe Dashboard → Webhooks: endpoint URL
# ${NEXT_PUBLIC_SITE_URL}/api/webhooks/stripe
